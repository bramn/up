<%= form_with(model: folder) do |form| %>
  <% if folder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(folder.errors.count, "error") %> prohibited this folder from being saved:</h2>

      <ul>
        <% folder.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :path %>
    <%= form.text_field :path %>
  </div>

  <div class="field">
    <%= form.label :base_name %>
    <%= form.text_field :base_name %>
  </div>

  <div class="field">
    <%= form.label :parent_folder %>
    <%= form.number_field :parent_folder %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
